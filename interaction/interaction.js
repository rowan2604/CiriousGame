class interaction {
    constructor(){
        this.object = []; //Array to store all objects.
        this.electricity;
        this.water;
        
        this.object.push({tiledActivate: [1,2,3], tiledDesactivate: [5,8,12], type: "Electricity", value: 10}); //Example of object.

    }

    update(tiled){}
    get(type){}

    /* Dev bellow */
}